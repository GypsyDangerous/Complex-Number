Complex c;
void setup()
{
  size(640, 480);
  c = new RandomComplex();
  //c.mult(2);
  //println(c.heading(), c.mag());
  //Complex cn = c.power(3);
  //println(cn.heading(), cn.mag());
  //println((c.heading() * 3) % TWO_PI);
}

void draw()
{
  background(51);
}
